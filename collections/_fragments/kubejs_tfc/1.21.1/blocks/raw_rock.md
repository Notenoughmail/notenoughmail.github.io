---
title: Raw Rock
anchor: raw-rock
name: raw_rock
type: tfc:raw_rock
cat:
    - kubejs_tfc
    - 1.21.1
    - block
example:
    - .uniqueSideTexture()
    - .stoneSoundType()
---

Creates a new raw rock block

Inherits the methods of the default block builder

{: #raw-rock-methods }

## Extra Method

- `.uniqueSideTexture()`{: .language-kube-21 #raw-rock-unique-side-texture }: Makes the default model generation use the `side` and `end` textures instead of just  the `end` texture
